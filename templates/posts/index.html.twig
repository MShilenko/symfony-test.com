{% extends 'base.html.twig' %}
	{% block title %} {% endblock %}
	{% block body %}
		<ul>
		{% for post in posts %}
			<li><a href="{{ path('blog_show', {'slug': post.slug}) }}">{{ post.title }}</a></li>
		{% endfor %}
		</ul>
	{% endblock %}